interface:
  display_name: "Git 提交"
  short_description: "整理改动范围并生成提交信息"
  default_prompt: "Use $commit-changes to inspect the current diff and prepare a safe commit."

policy:
  allow_implicit_invocation: false
