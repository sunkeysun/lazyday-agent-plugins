---
name: verify-change
description: 中文变更验证技能。用于验证当前代码改动、修复结果、任务完成度、测试失败是否已解决、构建或 lint 是否通过，以及交付前收集证据。适用于“验证一下”“跑测试”“确认修复是否有效”“交付前检查”“看这次改动是否完整”“验证当前 diff”等请求。默认只读验证，除非用户明确要求修复失败。
---

# 变更验证

## 目标

为当前改动或指定修复建立可信验证证据。验证要覆盖用户要求、原失败路径、关键回归路径和已知风险，不用宽泛命令掩盖真正缺口。

## 默认模式

默认只读验证。

允许：

- 读取代码、diff、测试、配置、日志和项目文档。
- 运行已有测试、类型检查、lint、构建、smoke 或只读诊断命令。
- 输出验证矩阵、通过证据、失败证据和剩余风险。

禁止：

- 未经明确要求修改文件、更新 snapshot、放宽测试或隐藏失败。
- 安装依赖、清理工作区、切换分支、提交代码或执行破坏性命令。
- 把未运行的检查写成已验证。

## 执行流程

1. 明确验证对象：当前 diff、指定文件、某个修复、某条失败路径或交付清单。
2. 读取适用项目规则和工作区状态。
3. 用 `git diff --stat`、`git diff --name-status` 和必要的局部 diff 建立变更地图。
4. 从变更地图推导最小验证集：测试、类型检查、lint、构建、smoke、日志或手工路径。
5. 优先验证原失败路径；其次验证受影响调用方和关键边界。
6. 运行验证并保留命令、结果和关键输出。
7. 如果验证失败，先分析失败是否来自本次改动、环境问题、既有失败或测试本身问题。

## 验证矩阵

输出时按需列出：

- 需求覆盖：每个用户目标是否有验证证据。
- 回归覆盖：相关调用方、契约、配置和边界是否检查。
- 自动化证据：命令、结果、关键输出。
- 手工证据：步骤、预期、实际结果。
- 未覆盖项：原因、风险、建议下一步。

## 失败处理

验证失败时不要直接修。先说明：

- 失败命令。
- 失败摘要。
- 初步归因。
- 是否阻塞交付。
- 最小修复或取证建议。

用户明确要求继续修复时，再交给 `implement-change` 或按当前任务授权进入实现。

## 输出

默认输出：

```markdown
**验证结果**
- 结论：
- 已运行：
- 通过证据：
- 失败或跳过：

**覆盖范围**
- 已覆盖：
- 未覆盖：
- 剩余风险：

**下一步**
- 
```

## 可选后续

- 验证失败需要定位：交给 `diagnose-problem`。
- 验证失败需要修复：交给 `implement-change`。
- 验证通过后需要提交：交给 `git-commit`。
